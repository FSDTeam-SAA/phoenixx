"use client";

const page = () => {
  // window.onload = function () { window.location.reload() }

  console.log("hello")

  return (
    <div>
      this is motivated video
    </div>
  );
};

export default page;